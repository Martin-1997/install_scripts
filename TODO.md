- set zsh as default shell for user (this does not work ATM)
- consider if you can make the VM more lightweight


~/.local/bin/claude is not linked to PATH correctly

link Claude code .dotfiles into Lima VM

setup dedicated ssh key and print it out at the end of the script


Allow setting username as a script parameter -> I think this may already work


